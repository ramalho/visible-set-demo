[project]
name = "visiset"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["numpy"]

[project.optional-dependencies]
dev = ["pytest"]

[tool.ruff.format]
quote-style = "single"

[tool.pytest.ini_options]
addopts = "--doctest-modules"
